{
  "version": 2,
  "framework": null,
  "installCommand": "pnpm install",
  "buildCommand": "pnpm --filter @workspace/api-server exec prisma generate && pnpm --filter @workspace/api-server exec prisma db push --skip-generate && pnpm --filter @workspace/api-server run build && pnpm --filter @workspace/api-server exec node --enable-source-maps ./dist/seed.mjs && pnpm --filter @workspace/academic-compass run build",
  "builds": [
    {
      "src": "frontend/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist/public" }
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "https://api.drumvale-academic-compass.co.ke/api/$1",
      "methods": ["GET", "POST", "PUT", "PATCH", "DELETE"]
    },
    {
      "src": "/(.*)",
      "dest": "/frontend/dist/public/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/frontend/dist/public/index.html",
      "method": ["GET"]
    }
  ]
}
